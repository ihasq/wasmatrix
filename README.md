# wasmatrix monorepo

This repository contains the WASMatrix package family.

- `wasmatrix`: AssemblyScript/WebAssembly SIMD matrix core and JavaScript
  runtime.
- `wasmatrix-gl`: glMatrix-compatible wrapper built from WASMatrix operation
  templates.
- `wasmatrix-gpu`: reserved package for a future wgpu-matrix-compatible wrapper.
